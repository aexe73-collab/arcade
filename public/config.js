// ArcadeFace config — set your Supabase credentials here
// Or set them as environment variables in Railway and serve them via /config endpoint
window.SUPABASE_URL      = "";   // e.g. "https://xxxx.supabase.co"
window.SUPABASE_ANON_KEY = "";   // your anon/public key
